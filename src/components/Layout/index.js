import SearchBar from '../SearchBar';
import Sidebar from '../Sidebar/index';
import './index.scss';

const Layout = () => {
	return (
		<>
			<Sidebar />
			<SearchBar />
		</>
	);
};

export default Layout;
